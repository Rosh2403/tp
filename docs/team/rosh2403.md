# Roshan Premil - Project Portfolio Page

## Project Overview

**RLAD (Record Losses And Debt)** is a minimalist Command-Line Interface (CLI) finance tracker built for power users who prefer speed and simplicity over GUI applications. Users can record, filter, sort, search, modify, and summarize financial transactions entirely from the terminal.

---

## Summary of Contributions

### Code Contributed

[tP Code Dashboard](https://nus-cs2113-ay2526-s2.github.io/tp-dashboard/?search=rosh2403)

---

### Features Implemented

#### 1. `DeleteCommand`
Removes a transaction permanently by its HashID.

- Parses the positional HashID argument from user input
- Validates that exactly one argument is provided (rejects extra arguments)
- Converts HashID to lowercase for case-insensitive matching
- Looks up the transaction via `TransactionManager.findTransaction()`
- Removes from both the `ArrayList` and `HashMap` in `TransactionManager`
- Shows a success message with the deleted transaction's details, or throws `RLADException` if not found

**Command format:** `delete <hashID>`

---

#### 2. `ModifyCommand`
Updates specific fields of an existing transaction without deleting and re-adding it.

- Accepts `field=value` pairs: `type`, `amount`, `date`, `category`, `description`
- Supports quoted values for multi-word descriptions (e.g., `description="Lunch with friends"`)
- Merges updated values with existing transaction fields — only provided fields are changed
- Validates field names against a whitelist and rejects unknown fields
- Validates type (must be `credit` or `debit`, case-insensitive), amount (positive, max $10M, 2 decimal places), and date (YYYY-MM-DD)
- Rebuilds the `Transaction` object and calls `setHashId()` to preserve the original ID
- Updates both the `ArrayList` and `HashMap` atomically via `TransactionManager.updateTransaction()`

**Command format:** `modify <hashID> field=value [field=value ...]`
**Example:** `modify a7b2c3 amount=25.00 description="New description"`

---

#### 3. `SummarizeCommand`
Provides a financial overview of transactions with optional filtering.

- Reuses `FilterCommand.buildPredicate()` to support the same filters as `list` (type, category, amount, date range)
- Uses `BigDecimal` for all aggregation to avoid floating-point display errors
- Calculates Total Credit, Total Debit, and Net Balance
- Groups transactions by category and shows sub-totals per category
- Handles empty state gracefully when no transactions match the filter

**Command format:** `summarize`, `summarize --type debit`, `summarize --date-from 2026-01-01 --date-to 2026-03-31`

---

#### 4. `SearchCommand`
Finds transactions by keyword across all searchable fields.

- Searches description, category, HashID, and amount (formatted as `$x.xx`)
- Case-insensitive matching using `.toLowerCase().contains()`
- Accepts a positional keyword argument directly after the command
- Also registered as `find` (alias) in the Parser
- Displays results in a formatted table identical in style to `ListCommand`

**Command format:** `search <keyword>`
**Example:** `search lunch`, `find food`

---

#### 5. `AmountValidator` (Shared Utility)
Extracted shared amount validation logic used by both `AddCommand` and `ModifyCommand` to eliminate code duplication.

- `validateFormat()` — rejects scientific notation (e.g., `1e5`) and amounts with more than 2 decimal places
- `parseAndValidate()` — full validation pipeline: format check, parse, positivity check, max limit ($10M), and rounding check (rejects amounts that round to $0.00)

---

### Bug Fixes (PE-D)

| Branch | Fix |
|---|---|
| `fix/delete-extra-args` | Reject extra arguments after HashID in delete command |
| `fix/modify-type-case` | Accept case-insensitive type values (e.g., `type=CREDIT`) in modify |
| `fix/reject-excess-decimals` | Reject amounts with >2 decimal places and scientific notation; extract `AmountValidator` |
| `fix/eof-infinite-loop` | Fix infinite loop when stdin reaches EOF (piped input) |
| `fix/budget-negative-display` | Display negative remaining budget correctly (e.g., `-$50.00`) with overspend warning |
| `fix/budget-set-overwrite-warning` | Reject budget set when budget already exists for that category |
| `fix/min-max-validation` | Validate min > max before filtering; replace sentinel values with boolean flags |
| `fix/invalid-type-filter` | Validate type filter value in `list` command (must be `credit` or `debit`) |
| `fix/empty-filter-key` | Skip empty filter keys instead of throwing unclear error |
| `fix/search-keyword-parsing` | Clean up search to use positional keyword argument |
| `fix/clear-force-flag` | Require `--force` flag exactly as documented in User Guide |
| `fix/sort-extra-args` | Reject extra arguments in sort command |
| `fix/import-export-legacy-flags` | Strip legacy `--file` flag in import/export for backward compatibility |
| `fix/storage-amount-validation` | Validate NaN/Infinity/negative amounts when loading from storage files |
| `fix/csv-import-zero-amount` | Reject zero-amount transactions during CSV import |

---

### Tests Written

| Test Class | Tests | What is covered |
|---|---|---|
| `DeleteCommandTest` | 13 | Valid deletion, success message, missing/null/invalid ID, extra arguments rejection, correct transaction removal, empty manager |
| `ModifyCommandTest` | 12 | Single/multiple field updates, type/amount/date modification, invalid ID, no fields, invalid type/amount, case-insensitive type, amount rounding, HashID preservation |
| `SummarizeCommandTest` | 6 | Credit/debit totals, category grouping, null category, floating-point precision, empty state |
| `SearchCommandTest` | 9 | Search by description/category/amount/ID, case-insensitivity, empty results, result count |
| `FilterCommandTest` | 41 | Type/category/amount/date filtering, partial match, multiple categories, category codes, relative dates, date ranges, invalid inputs, colon-style filters, type validation |

---

### Contributions to Developer Guide

Authored the following sections in `DeveloperGuide.md`:

- **DeleteCommand**: Sequence diagram showing argument parsing, `findTransaction()`, `deleteTransaction()`, and `BudgetManager` notification chain
- **ModifyCommand**: Sequence diagram showing field merging, `setHashId()`, `updateTransaction()`, and `BudgetManager` notification chain; design notes on atomic ArrayList + HashMap update
- **SummarizeCommand**: Sequence diagram showing `FilterCommand.buildPredicate()` reuse, `BigDecimal` aggregation loop, and category grouping

---

### Contributions to User Guide

- Fixed UG formatting: aligned tables, fixed spacing, reordered sections
- Updated README with new command options and usage

---

### Contributions to Team Tasks

- Extracted `AmountValidator` shared utility to eliminate duplicated validation logic across `AddCommand` and `ModifyCommand`
- Added `setHashId()` to `Transaction` to support ID-preserving updates in `ModifyCommand`
- Resolved multiple merge conflicts when syncing fix branches with upstream master
- Fixed DG diagram readability with larger fonts and higher contrast
- Fixed PDF formatting with print CSS and updated outdated `index.md`
- Removed accidentally committed heap dump and added `.hprof` to `.gitignore`
- Added thousands separators to all displayed amounts for readability
